
&НаСервере
Процедура ОтсортироватьНаСервере()
	
	ТекОбъект = РеквизитФормыВЗначение("Объект");
	ТекОбъект.ОтсортироватьМассив();
	ЗначениеВРеквизитФормы(ТекОбъект, "Объект");
	
КонецПроцедуры

&НаКлиенте
Процедура Отсортировать(Команда)
	
	Если Объект.СортируемыйМассив.Количество() > 0 Тогда
		ОтсортироватьНаСервере();
	КонецЕсли;
	
КонецПроцедуры
